```{r}
html_esc <- function(x) {
  x <- gsub("<", "&lt;", x)
  x <- gsub(">", "&gt;", x)
}
knitr::knit_hooks$set(fansi = function(before, options, envir) {
  if (before) fansi::sgr_to_html(x = html_esc(x))
})
```
